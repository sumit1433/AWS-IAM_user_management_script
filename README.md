# AWS-IAM_user_management_script
A Bash script that automatically creates AWS IAM users from a list, sets their login passwords, attaches required IAM policies and permission boundaries, creates a DevOps group, and adds all users to that group and the aws_auto_del script delete IAM users and IAM group by detaching all the policies present in the script.
